# Cache-Busting für Flutter-Dateien
<FilesMatch "\.(dart\.js|main\.dart\.js|flutter\.js|flutter_bootstrap\.js|index\.html)$">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
</FilesMatch>

# Zusätzliche Sicherheit für alle JS-Dateien im Flutter-Build
<FilesMatch "\.js$">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
</FilesMatch> 